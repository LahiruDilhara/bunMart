import { ProfileView } from "@/profile/ProfileView";

export const metadata = {
  title: "My Profile - BunMart",
  description: "Manage your BunMart profile, orders, addresses and subscriptions",
};

export default function ProfilePage() {
  return <ProfileView />;
}